# blueprint/video-digest - run log

## Learnings

- rh_run collect mode bounds output (live-verified).
- one model call per chunk keeps failures contained (text family).
- yt-dlp needs no key - it is the local half of the pipeline.