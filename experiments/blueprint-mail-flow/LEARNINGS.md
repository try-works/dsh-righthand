# blueprint/mail-flow - run log

## Learnings

- the inbox-triage kit is the triage half, unchanged.
- exec collect bounds are live-verified.
- this is the keyless-rule edge: the user's own account via their own local config, no third-party token.