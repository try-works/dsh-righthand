{
  "name": "blueprint/deck-pipeline",
  "kind": "blueprint",
  "description": "Decks as artifacts: python-pptx builds the deck, R2 publishes it, a presigned link ships it",
  "hermes_source": "powerpoint",
  "ingredients": ["rh_run python-pptx build", "rh_files_put the deck", "rh_store deck:<slug> index", "rh_files_share + rh_notify_send"],
  "recipe": "build the deck from the source text -> exit 0 gates the put -> publish, index, share -> notify the link",
  "pitfalls": ["the deck source is text - keep it beside the render receipt", "exit 0 gates the put", "the presigned link expires"],
  "related": ["rh_store_*"],
  "template": { "spec": "blueprint.json", "recipe": "README.md", "learnings": "LEARNINGS.md" }
}