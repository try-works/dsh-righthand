# blueprint/device-ping - run log

## Learnings

- heartbeat shipped the receipt + streak shape (live-verified curl runs).
- a deliberate boundary: local reachability belongs to rh_run, arbitrary URLs to guardedFetch.